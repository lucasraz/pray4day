# Orchestration Layer (Layer 2)

This folder contains application decision and orchestration code (Application layer).
It reads directives and implements their flows through backend services, coordinators, controllers, and error/job handling.
It enforces validation, authorization, and tenant boundaries.
